# Normalize line endings: text files use LF in the repo.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets.
*.jar binary
*.png binary
*.ico binary
